{
  "name": "niivue-niimath",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp *.gz dist && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.9.0"
  },
  "dependencies": {
    "@niivue/niimath-js": "^0.0.1643874671203",
    "@niivue/niivue": "^0.38.1"
  }
}
